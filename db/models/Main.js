var sequelize = require('sequelize');

var Main = sequelize.define('msrw_main', {
    id: {
        type: Sequelize.INTEGER
    },
    createdate: {
        type: Sequelize.DATE
    },
    changedate:{
        type: Se
    }
});